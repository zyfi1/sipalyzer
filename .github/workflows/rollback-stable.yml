name: Rollback Main Release

on:
  workflow_dispatch:
    inputs:
      rollback_to_tag:
        description: "Known-good tag to roll back to (e.g. v1.2.2 or v1.2.3-rc.2)"
        required: true
        type: string
      new_main_tag:
        description: "New main tag to publish for rollback (e.g. v1.2.4)"
        required: true
        type: string

permissions:
  contents: write

jobs:
  rollback:
    name: Roll back main channel
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Validate rollback request
        id: validate
        shell: bash
        run: |
          set -euo pipefail
          SOURCE="${{ inputs.rollback_to_tag }}"
          TARGET="${{ inputs.new_main_tag }}"
          git fetch --tags

          git rev-parse "${SOURCE}^{tag}" >/dev/null
          if [[ ! "${TARGET}" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "new_main_tag must be final format: vX.Y.Z" >&2
            exit 1
          fi
          if git rev-parse "${TARGET}^{tag}" >/dev/null 2>&1; then
            echo "new_main_tag already exists: ${TARGET}" >&2
            exit 1
          fi

          SOURCE_COMMIT="$(git rev-list -n 1 "${SOURCE}")"
          echo "source_commit=${SOURCE_COMMIT}" >> "$GITHUB_OUTPUT"

      - name: Create rollback main tag
        shell: bash
        run: |
          set -euo pipefail
          git tag -a "${{ inputs.new_main_tag }}" "${{ steps.validate.outputs.source_commit }}" \
            -m "Rollback main to ${{ inputs.rollback_to_tag }}"
          git push origin "${{ inputs.new_main_tag }}"

      - name: Rollback summary
        shell: bash
        run: |
          echo "Rollback published: ${{ inputs.new_main_tag }} -> commit from ${{ inputs.rollback_to_tag }}"

